name: merlin_editor
description: "Create Shmup games with ease with Flutter and Flame"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  equatable: ^2.0.5
  file_selector: ^1.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  merlin:
    path: packages/merlin
  nes_ui: ^0.18.0
  path: ^1.8.3

dev_dependencies:
  bloc_test: ^9.1.6
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
